version: '3'

services:
  lipstick-client:
    build:
      context: .
      dockerfile: Dockerfile
    image: jliotorresmoreno/lipstick:latest
    entrypoint: /entrypoint.sh
    command: [ "client" ]
    network_mode: host
    
